[Unit]
Description=Run podman-stats forwarder (writes JSON lines to /srv/monitoring_data/promtail/podman_stats.log)
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/podman-stats-forward.sh

[Install]
WantedBy=multi-user.target
